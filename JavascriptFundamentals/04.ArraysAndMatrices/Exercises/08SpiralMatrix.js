function spiralMatrix(a, b) {
    let index = 1;
    let output = [[]];

    for (let i = 0; i < b; i++) {
        index++;
        output[1] = i;
    }

    for (let row = 1; row < a; row++) {
    }
}